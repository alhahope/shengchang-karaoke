# 参与贡献

感谢你改进“声场”。

## 开始开发

1. Fork 或克隆仓库。
2. 使用 Node.js 22.13 或更高版本。
3. 运行 `npm install` 和 `npm run dev`。
4. 修改后运行 `npm test`。

## 提交建议

- 一个提交只解决一个清晰的问题。
- 不要提交歌曲、MV、歌词、录音、密钥或个人站点编号。
- 涉及音频路由时，请同时考虑 Windows Chrome/Edge 和 macOS Chrome。
- 涉及 MKV/MPG 时，请说明文件的音轨数量、声道数和编码格式；不要上传无权分享的媒体。
- UI 修改应保持键盘可操作并兼顾窄屏布局。

## 报告问题

请说明浏览器和系统版本、媒体格式、复现步骤和实际表现。涉及私人歌库时，可只提供 `ffprobe` 结构信息，不必分享歌曲文件。
